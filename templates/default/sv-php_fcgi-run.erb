#!/bin/bash

cd <%= @options[:app].path %>/current

exec 2>&1
PHP_FCGI_CHILDREN=<%= @options[:app].worker_processes %> PHP_FCGI_MAX_REQUESTS=<%= @options[:app].worker_max_requests %> exec chpst -u <%= @options[:app].owner %>:<%= @options[:app].group %> php-cgi -b 127.0.0.1:<%= @options[:app].worker_port %>
